{
  "buildCommand": "next build && npm run build:sanity",
  "installCommand": "npm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/studio/:path*",
      "destination": "/:path*"
    }
  ]
}
